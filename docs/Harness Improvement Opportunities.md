# Harness Improvement Opportunities

Ranked backlog for context-management and bridge-tool improvements found while reviewing Mr. Flame / router activity. Items are deduped across heartbeat notes and ranked by expected token-efficiency impact.

Implementation note, 2026-05-26: the highest-impact bridge surfaces now exist for restarted runtimes: `observe_state_if_changed_XS` / `_XXS`, compact movement/object aliases, `object_transition_step_XS` / `_XXS`, `combat_cleanup_XS` / `_XXS`, `wait_until_combat_event_smart_XS` / `_XXS`, and `combat_restock_trip_XS` / `_XXS`. The original "add compact combat cleanup" item is now retired because `combat_cleanup_XS` is active in the live loop; remaining work is mainly runner adoption, live rollout validation, and follow-up improvements where compact outputs still omit decision fields.

## Highest Impact

1. **Suppress Repeated Full Observations**

   - **Evidence:** `observe_state` repeatedly dominated hourly payload spend: one audited hour returned about 8.85M JSON characters across 608 calls, and another returned about 9.9M across 680 calls. Bursts of nearly identical 14K-15K full observations appeared after kills, route legs, and cleanup actions. After compact tools became available, the 2026-05-26T16:47Z Mr. Flame session still made three full `observe_state` calls in seven tool results, including repeated bank-stable observations, so runner-side suppression/defaulting is still needed. In the 2026-05-26T17:26Z-17:55Z live window, the agent still made 22 full `observe_state` calls at about 15.0K characters each despite also using `observe_state_if_changed_XXS`. In the 2026-05-26T17:55Z-18:50Z window, another 33 full `observe_state` results averaged about 15.3K characters, mostly around bank/restock routing bursts where compact state was already available. In the 2026-05-26T18:50Z-19:50Z window, full observe volume improved to 19 calls but still cost about 292K JSON characters. In 2026-05-26T19:50Z-20:50Z, full observe fell again to eight calls, but those still cost about 124K JSON characters; most routine state checking now used `observe_state_if_changed_XXS`. In the 2026-05-26T20:50Z-21:52Z window, full observe rose back to 19 calls and about 293K characters, showing bank/restock and route returns still leak legacy observation payloads. In the 2026-05-26T21:52Z-22:51Z window, full observe rose again to 30 calls and about 450K characters. In 2026-05-26T22:51Z-23:50Z, it improved to 16 calls but still cost about 240K characters, mostly around restock transitions. In 2026-05-26T23:50Z-2026-05-27T00:50Z, it fell to 11 calls and about 165K characters, but the remaining calls still appear around full movement/restock phases.
   - **Recent evidence:** In 2026-05-27T00:50Z-02:50Z, full `observe_state` rose to 36 calls and about 536K characters, mostly around movement/restock phases where compact state was still available. In 2026-05-27T02:50Z-03:50Z, it still made 19 calls for about 285K characters. In 2026-05-27T03:50Z-04:50Z, it rose again to 25 calls and about 377K characters. In 2026-05-27T04:50Z-05:50Z, a gear/shop upgrade loop made 78 full `observe_state` calls for about 1.10M characters, mostly around banking, shopping, and route checks where full bank/equipment evidence was not needed every step. In 2026-05-27T05:50Z-06:50Z, the live loop still made 70 full `observe_state` calls for about 1.02M characters, including repeated bank/restock recovery checks and return-to-combat route checks. In 2026-05-27T07:01Z-07:50Z, object-transition routing made 118 full `observe_state` calls for about 1.76M characters. In 2026-05-27T07:50Z-08:50Z, the same route/combat run made 142 full observes for about 2.20M characters despite also using `observe_state_XXS` 36 times. In 2026-05-27T08:50Z-09:50Z, full observe regressed further to 198 calls and about 3.14M characters, while `observe_state_XXS` appeared only 55 times. In 2026-05-27T09:50Z-10:50Z, full observe stayed high at 184 calls and about 2.96M characters, with only 35 `observe_state_XXS` calls. In 2026-05-27T10:50Z-11:50Z, full observe rose again to 230 calls and about 3.66M characters, while `observe_state_XXS` appeared only 44 times. In 2026-05-27T11:50Z-12:50Z, full observe remained high at 191 calls and about 3.08M characters, with only 50 `observe_state_XXS` calls. In 2026-05-27T12:50Z-13:50Z, full observe still cost about 2.65M characters across 161 calls, while `observe_state_XXS` appeared 47 times.
   - **Latest audit:** In 2026-05-27T13:50Z-14:50Z, full `observe_state` rose to 178 calls and about 2.92M characters, while `observe_state_XXS` appeared only 37 times. In 2026-05-27T14:50Z-15:50Z, full observe eased to 138 calls but still cost about 2.29M characters, with only 35 `observe_state_XXS` calls. In 2026-05-27T16:10Z-16:50Z, full `observe_state` was again the top payload source with 141 calls and about 2.26M characters; many occurred immediately after compact cleanup or route arrivals where compact state was already returned. In 2026-05-27T16:50Z-19:24Z, full `observe_state` remained the largest payload source with 355 calls and about 5.72M characters; 322 compact cleanup results were followed by a full observe within the next three tool results. In 2026-05-27T19:24Z-21:24Z, full `observe_state` still dominated with 278 calls and about 4.52M characters; 254 compact cleanup results were followed by a full observe within the next three tool results, and 141 adjacent full-observe pairs appeared.
   - **Fresh audit:** In 2026-05-27T21:24Z-23:24Z, full `observe_state` again dominated returned payload with 288 calls and about 4.65M characters; 264 compact cleanup results were followed by a full observe within the next three tool results, and 147 adjacent full-observe pairs appeared. In 2026-05-27T23:24Z-2026-05-28T01:24Z, full observe remained the top payload source with 296 calls and about 4.79M characters; 264 compact cleanup results were followed by a full observe within the next three tool results, and 151 adjacent full-observe pairs appeared.
   - **Change:** Add a compact `stateHash` / `changedSinceLast` field, or an `observe_state_if_changed_XS` tool that returns only critical state when bulky fields have not changed. Also add agent-side suppression for repeated full observes inside a short window unless HP, combat, inventory, interface, or location changed.
   - **Expected efficiency:** Very high. This attacks the largest repeated payload source directly while preserving full evidence when state actually changes.

2. **Make Routine Movement And Router Steps Compact By Default**

   - **Evidence:** `walk_to_tile_until_arrived` returned about 1.79M JSON characters across 126 calls in one window, averaging about 14.2K per call. Router/object movement also used full-output `walk_to_tile_until_arrived`, `interact_object`, `walk_path_steps`, `wait_until_idle`, and surrounding full observes even when only arrival/critical state was needed. In the 2026-05-26T17:26Z-17:55Z window, the live agent added 30 more full `walk_to_tile_until_arrived` results, averaging about 15.2K characters each, while no matching `_XS` or `_XXS` movement calls appeared in that window. The 2026-05-26T17:55Z-18:50Z window added 45 more full movement results averaging about 15.5K characters, still with no compact movement adoption. The 2026-05-26T18:50Z-19:50Z window added 30 more full movement results for about 466K JSON characters; movement remains the biggest legacy surface not yet adopted. The 2026-05-26T19:50Z-20:50Z window improved to 14 full movement results but still spent about 219K JSON characters, more than all full observations in that window. In 2026-05-26T20:50Z-21:52Z, full movement regressed to 31 calls and about 484K characters, again the largest legacy surface. In 2026-05-26T21:52Z-22:51Z, movement stayed high at 45 full calls and about 682K characters, becoming the largest single legacy payload source. In 2026-05-26T22:51Z-23:50Z, full movement improved to 31 calls but still consumed about 470K characters, more than any non-wait tool family. In 2026-05-26T23:50Z-2026-05-27T00:50Z, full movement dropped to 15 calls but still consumed about 228K characters and remained the largest legacy action surface.
   - **Recent evidence:** In 2026-05-27T00:50Z-02:50Z, full `walk_to_tile_until_arrived` climbed to 61 calls and about 919K characters, again the largest legacy surface and larger than full observe. In 2026-05-27T02:50Z-03:50Z, it remained the top legacy surface with 31 calls and about 472K characters. In 2026-05-27T03:50Z-04:50Z, it increased to 39 calls and about 591K characters. In 2026-05-27T04:50Z-05:50Z, the gear/shop route made 206 full `walk_to_tile_until_arrived` calls for about 2.85M characters, dwarfing all compact combat tooling in the same window. In 2026-05-27T05:50Z-06:50Z, full movement stayed high at 64 calls and about 942K characters; compact movement was used only twice. In 2026-05-27T07:01Z-07:50Z, the Varrock/Edgeville-dungeon object route used 117 full `walk_to_tile_until_arrived` calls for about 1.67M characters, plus 37 full `preview_local_path` calls, 21 full `wait_until_idle` calls, 19 full `interact_object` calls, and 12 full `walk_path_steps` calls. In 2026-05-27T07:50Z-08:50Z, object-route movement remained the largest source again: 140 full movement calls for about 2.19M characters, plus 39 full `wait_until_idle`, 39 full `interact_object`, 28 full `walk_path_steps`, and 21 full `wait_ticks` calls. In 2026-05-27T08:50Z-09:50Z, movement and route polling dominated the regression: 298 full `walk_to_tile_until_arrived` calls cost about 4.79M characters, 75 full `wait_ticks` calls about 1.19M, 60 full `wait_until_idle` calls about 964K, 60 full `interact_object` calls about 536K, and 44 full `walk_path_steps` calls about 389K. In 2026-05-27T09:50Z-10:50Z, short route legs still used full output: 333 `walk_to_tile_until_arrived` calls cost about 5.40M characters, including 175 `max_ticks_reached` and two oscillations; full `wait_ticks`, `wait_until_idle`, `interact_object`, and `walk_path_steps` added another about 1.97M characters. In 2026-05-27T10:50Z-11:50Z, route movement became the largest hourly payload seen in recent audits: 748 full `walk_to_tile_until_arrived` calls cost about 12.15M characters, with 554 `max_ticks_reached`; many legs used `maxTicks:1`, `maxTicks:2`, or `maxTicks:3` to the same repeated dungeon coordinates. Full `wait_until_idle`, `interact_object`, `wait_ticks`, and `walk_path_steps` added about 1.99M more characters. In 2026-05-27T11:50Z-12:50Z, full movement increased again to 865 calls and about 14.32M characters; 704 returned `max_ticks_reached`, and every high-volume target leg used `maxTicks:2` against repeated dungeon coordinates. Full `wait_until_idle`, `wait_ticks`, `interact_object`, and `walk_path_steps` added about 2.08M more characters. In 2026-05-27T12:50Z-13:50Z, full movement eased but still dominated: 626 `walk_to_tile_until_arrived` calls cost about 10.51M characters, 467 returned `max_ticks_reached`, and all high-volume route legs still used `maxTicks:2`; full `wait_ticks`, `wait_until_idle`, `interact_object`, and `walk_path_steps` added about 2.10M more characters.
   - **Latest audit:** In 2026-05-27T13:50Z-14:50Z, full `walk_to_tile_until_arrived` again dominated payload spend with 845 calls and about 14.26M characters; 633 returned `max_ticks_reached`, and the highest-volume legs still used `maxTicks:2`. Full `wait_until_idle`, `wait_ticks`, `walk_path_steps`, and `interact_object` added about 2.51M more characters. In 2026-05-27T14:50Z-15:50Z, full movement stayed the largest surface with 531 calls and about 9.02M characters; full `wait_ticks` surged to 251 calls and about 4.14M characters, and full `wait_until_idle`, `walk_path_steps`, and `interact_object` added about 1.03M more. In 2026-05-27T16:10Z-16:50Z, full route execution still used 41 full `walk_to_tile_until_arrived` calls for about 653K characters, and 44 full `wait_ticks` calls added about 714K characters despite compact wait/movement variants existing. In 2026-05-27T16:50Z-19:24Z, full movement eased but did not disappear: 63 `walk_to_tile_until_arrived` calls cost about 1.03M characters, and nine full `wait_ticks` calls added about 147K. In 2026-05-27T19:24Z-21:24Z, full movement continued at 45 `walk_to_tile_until_arrived` calls for about 737K characters, with four full `wait_ticks` adding about 66K.
   - **Fresh audit:** In 2026-05-27T21:24Z-23:24Z, routine movement still used legacy full output: 45 `walk_to_tile_until_arrived` calls cost about 738K characters, and eight full `wait_ticks` calls added about 131K characters. In 2026-05-27T23:24Z-2026-05-28T01:24Z, legacy movement rose to 60 full `walk_to_tile_until_arrived` calls for about 979K characters, with nine full `wait_ticks` adding about 147K.
   - **Change:** Update route/restock runners, script primitives, and active-agent guidance so routine movement uses `_XXS` by default and `_XS` when route evidence is needed. Reserve legacy full movement tools for explicit debug/full-state requests. Add or adopt compact `interact_object_XS` / `_XXS`, or an `object_transition_step_XS` that performs approach, click, and post-click idle wait with compact status.
   - **Expected efficiency:** Very high. Movement/restock/router loops are common and full movement payloads are nearly as large as full observations.

3. **Make Combat Waits Target-Aware And Event-Rich**

   - **Evidence:** In a white knight loop, `wait_until_combat_event_XS` completed 142 times and returned about 539K JSON characters; 93 completions were `max_ticks_reached` and 50 were `target_dead`. Earlier loops also returned `target_dead` before loot was visible, causing extra `combat_state_XS` and full `observe_state` polling. After the smart XS variant was adopted, the 2026-05-26T17:26Z-17:55Z window still had 159 `wait_until_combat_event_smart_XS` calls returning about 618K characters, with many `max_ticks_reached` results before `loot_appeared`; this suggests the wait policy still needs longer target-aware waits or XXS polling when only survival state is needed. In the 2026-05-26T18:50Z-19:50Z window, `wait_until_combat_event_smart_XS` was the top tool again with 239 calls and about 930K JSON characters. In the 2026-05-26T19:50Z-20:50Z window, it remained the top payload source with 252 calls and about 992K JSON characters, even though full-tool payloads were much lower. In 2026-05-26T20:50Z-21:52Z, it stayed flat at 244 calls and about 949K characters, so XS wait payload now dominates total compact context. In 2026-05-26T21:52Z-22:51Z, it remained high at 225 calls and about 849K characters. In 2026-05-26T22:51Z-23:50Z, it was still the top payload source with 231 calls and about 886K characters. In 2026-05-26T23:50Z-2026-05-27T00:50Z, it stayed at 234 calls and about 898K characters, so compact wait payload is now the most consistent hourly spend.
   - **Recent evidence:** In 2026-05-27T00:50Z-02:50Z, `wait_until_combat_event_smart_XS` made 468 calls for about 1.79M characters; 349 returned `max_ticks_reached` even though every call used `autoExtendWhileSafe=true` and `maxTicks=20`. In 2026-05-27T02:50Z-03:50Z, it made another 236 calls for about 894K characters, with 165 `max_ticks_reached`. In 2026-05-27T03:50Z-04:50Z, it stayed flat at 234 calls and about 886K characters, with 161 `max_ticks_reached`. In 2026-05-27T05:50Z-06:50Z, it made 194 calls for about 707K characters, with 138 `max_ticks_reached`; the call rate improved but mid-fight polling still dominated compact payload. In 2026-05-27T07:50Z-08:50Z, Moss giant combat made 137 smart wait calls for about 488K characters, with 118 `max_ticks_reached`; most of these used short `maxTicks:8` and `autoExtendWhileSafe:false`, suggesting the runner is deliberately polling too often. In 2026-05-27T08:50Z-09:50Z, the same Moss giant loop made 254 smart wait calls for about 909K characters; all calls used `maxTicks:8` and `autoExtendWhileSafe:false`, and 212 returned `max_ticks_reached`. In 2026-05-27T09:50Z-10:50Z, the wait loop worsened to 524 smart XS calls and about 1.85M characters; 294 calls used only `maxTicks:3`, 230 used `maxTicks:8`, and 457 returned `max_ticks_reached`. In 2026-05-27T10:50Z-11:50Z, the wait loop became the second-largest payload source: 974 smart XS calls cost about 3.18M characters, 949 returned `max_ticks_reached`, and most used `maxTicks:1` or `maxTicks:3` with `autoExtendWhileSafe:false`. In 2026-05-27T11:50Z-12:50Z, the wait loop more than doubled again: 2,465 smart XS calls cost about 7.96M characters; every call used `maxTicks:1` with `autoExtendWhileSafe:false`, and 2,423 returned `max_ticks_reached`. In 2026-05-27T12:50Z-13:50Z, the same one-tick wait policy continued with 1,789 smart XS calls and about 5.75M characters; 1,759 returned `max_ticks_reached`.
   - **Latest audit:** In 2026-05-27T13:50Z-14:50Z, `wait_until_combat_event_smart_XS` dropped to 677 calls but still returned about 2.13M characters; 659 calls returned `max_ticks_reached`, every call used `maxTicks:1`, and all used `autoExtendWhileSafe:false`. In 2026-05-27T14:50Z-15:50Z, it made 737 calls for about 2.38M characters; 721 returned `max_ticks_reached`, and every call again used `maxTicks:1` with `autoExtendWhileSafe:false`. In 2026-05-27T16:10Z-16:50Z, the wait policy improved to `autoExtendWhileSafe:true` and `maxTicks:20`, but 50 calls still returned about 163K characters, with 44 `max_ticks_reached` results and full compact skill maps in each response. In 2026-05-27T16:50Z-19:24Z, the improved wait policy continued, but 488 smart XS waits still returned about 1.83M characters; 344 were `max_ticks_reached`, 127 were `loot_appeared`, and the no-event responses still carried broad compact player/skills state. In 2026-05-27T19:24Z-21:24Z, smart waits made 389 calls for about 1.46M characters; 275 were `max_ticks_reached`, 104 were `loot_appeared`, and nearly all used `maxTicks:20` with `autoExtendWhileSafe:true`, so the remaining waste is response size rather than one-tick polling.
   - **Fresh audit:** In 2026-05-27T21:24Z-23:24Z, `wait_until_combat_event_smart_XS` returned about 1.56M characters across 414 calls; 292 were `max_ticks_reached`, 108 were `loot_appeared`, and nearly all used `maxTicks:20` with `autoExtendWhileSafe:true`, confirming response size is now the main wait-loop waste. In 2026-05-27T23:24Z-2026-05-28T01:24Z, it returned about 1.65M characters across 442 calls; 331 were `max_ticks_reached`, 97 were `loot_appeared`, and the policy stayed at `maxTicks:20` with `autoExtendWhileSafe:true`.
   - **Change:** Add `untilTargetDead` / `autoExtendWhileSafe`, or return `recommendedMaxTicks` based on target HP/max hit, player HP, food, and retreat thresholds. Include `lootPending`, `deathTile`, `groundItemCount`, `recommendedWaitTicks`, and `inventory_pressure` / `inventory_full` events. Suppress unchanged full skill/player snapshots in high-frequency no-event results and rely on `skillChanges` / `xpRecent` for XP deltas. XXS should expose only status plus critical survival state.
   - **Expected efficiency:** Very high. It reduces repetitive mid-fight waits and post-death polling without hiding safety exits.

4. **Add A Compact Combat Restock Trip Primitive**

   - **Evidence:** Restock cycles repeatedly combined full-output movement, object transitions, bank actions, withdraw-mode button clicks, food withdrawal, and return routing. One audited hour included 36 `walk_to_tile_until_arrived`, 21 `interact_object`, 16 `walk_path_steps`, 22 `wait_until_idle`, 32 `click_interface_button`, and multiple bank calls. During 2026-05-26T18:45Z-18:47Z, two short bank/restock bursts alone produced repeated full `observe_state`, full `walk_to_tile_until_arrived`, full `deposit_inventory_items`, and paired full `click_interface_button` calls before returning to combat. In the 2026-05-26T20:50Z-21:52Z window, restock-adjacent full calls still included 31 full movement calls, 19 full observes, eight full `click_interface_button` calls, two full withdrawals, two full deposits, and one full excess-coin deposit. In 2026-05-26T21:52Z-22:51Z, restock/bank handling again included 45 full movement calls, 30 full observes, 12 full `click_interface_button` calls, six full deposits, one full withdrawal, and one full excess-coin deposit. In 2026-05-26T22:51Z-23:50Z, restock handling improved but still used 31 full movement calls, 16 full observes, eight full `click_interface_button` calls, two full withdrawals, two full deposits, and one full excess-coin deposit. In 2026-05-26T23:50Z-2026-05-27T00:50Z, restock improved again but still used 15 full movement calls, 11 full observes, four full `click_interface_button` calls, one full deposit, and one full excess-coin deposit.
   - **Recent evidence:** In 2026-05-27T00:50Z-02:50Z, restock-adjacent legacy calls included 61 full movement calls, 36 full observes, 16 full withdraw-mode button clicks, four full deposits, two full withdrawals, and two full excess-coin deposits. In 2026-05-27T02:50Z-03:50Z, the same pattern continued with 31 full movement calls, 19 full observes, eight full withdraw-mode clicks, two full deposits, and one full withdrawal. In 2026-05-27T03:50Z-04:50Z, restock leakage grew again with 39 full movement calls, 25 full observes, 10 full withdraw-mode clicks, three full deposits, one full withdrawal, and one full excess-coin deposit. In 2026-05-27T04:50Z-05:50Z, a gear-upgrade trip reused the same full bank/movement pattern: 206 full movement calls, 78 full observes, 12 full deposits, eight full withdrawals, 10 full bank-mode clicks, and 15 full close-interface calls. In 2026-05-27T05:50Z-06:50Z, restock/gear recovery still leaked 64 full movement calls, 70 full observes, 16 full bank-mode clicks, three full deposits, two full withdrawals, two full excess-coin deposits, and three full close-interface calls. In 2026-05-27T16:50Z-19:24Z, restock/bank surfaces still leaked 63 full movement calls, 355 full observes, 16 full bank-mode clicks, 20 full deposits, four full withdrawals, and two full excess-coin deposits. In 2026-05-27T19:24Z-21:24Z, the same restock surface still leaked 45 full movement calls, 278 full observes, 12 full bank-mode clicks, 15 full deposits, three full withdrawals, and one full excess-coin deposit.
   - **Fresh audit:** In 2026-05-27T21:24Z-23:24Z, the same restock surface still leaked 45 full movement calls, 288 full observes, 12 full bank-mode clicks, 15 full deposits, three full withdrawals, and two full excess-coin deposits. In 2026-05-27T23:24Z-2026-05-28T01:24Z, restock leakage increased to 60 full movement calls, 296 full observes, 16 full bank-mode clicks, 20 full deposits, three full withdrawals, and two full excess-coin deposits.
   - **Change:** Add a primitive-backed `combat_restock_trip_XS` runner or bridge batch that executes route-to-bank, deposit policy, food withdrawal, optional excess-coin deposit, and return route with compact phase/status updates.
   - **Expected efficiency:** Very high. Restock trips are long, repetitive, and currently spread context across many large tool results.

## Medium Impact

1. **Batch Combat Target Acquisition**

   - **Evidence:** One hour added 49 `find_training_npc` calls and 49 `attack_npc` calls. `attack_npc` averaged about 7.6K JSON characters and routine results were usually "Found suitable combat training NPC" followed by "Attacking White Knight" or a short walk-into-range message. After `attack_npc_XXS` adoption, the 2026-05-26T17:55Z-18:50Z window still showed four `attack_npc_XXS` unreachable failures followed by `find_training_npc` recovery and another attack attempt. The 2026-05-26T18:50Z-19:50Z window added eight more `attack_npc_XXS` reachability failures and eight `find_training_npc` calls, showing target-acquisition recovery is now the main combat-loop gap rather than attack payload size. In the 2026-05-26T19:50Z-20:50Z window, five more reachability failures and five `find_training_npc` calls repeated the same recovery pattern. In 2026-05-26T20:50Z-21:52Z, six more `attack_npc_XXS` reachability failures and six `find_training_npc` calls continued the same pattern. In 2026-05-26T21:52Z-22:51Z, reachability failures dropped to three and only three `find_training_npc` calls were needed, so target recovery improved while movement/restock remained the bigger waste. The 2026-05-26T22:51Z-23:50Z window repeated that improved pattern with three reachability failures and three `find_training_npc` calls. The 2026-05-26T23:50Z-2026-05-27T00:50Z window again had three reachability failures and three `find_training_npc` calls.
   - **Recent evidence:** In 2026-05-27T00:50Z-02:50Z, `attack_npc_XXS` produced 12 unreachable failures and the agent made 12 `find_training_npc` calls, so the failure-recovery pair is still repeating. In 2026-05-27T02:50Z-03:50Z, this eased to four unreachable failures and four `find_training_npc` calls but did not disappear. In 2026-05-27T03:50Z-04:50Z, it eased again to three failures and three `find_training_npc` calls. In 2026-05-27T05:50Z-06:50Z, the agent made 51 `find_training_npc` calls for about 91K characters while `attack_npc_XXS` succeeded, showing routine target selection still costs a separate compact-but-not-XXS call per kill cycle. In 2026-05-27T16:10Z-16:50Z, the agent made 18 `find_training_npc` calls and 18 `attack_npc_XXS` calls, so the two-call acquisition/attack pattern remains active even when the attack result itself is compact. In 2026-05-27T16:50Z-19:24Z, the pattern scaled up again to 161 `find_training_npc` calls for about 279K characters and 161 `attack_npc_XXS` calls. In 2026-05-27T19:24Z-21:24Z, target acquisition still ran as 128 `find_training_npc` calls for about 226K characters plus 128 `attack_npc_XXS` calls.
   - **Fresh audit:** In both 2026-05-27T21:24Z-23:24Z and 2026-05-27T23:24Z-2026-05-28T01:24Z, target acquisition still ran as 132 `find_training_npc` calls plus 132 `attack_npc_XXS` calls. The latest window spent about 233K characters on target finding; 127 attacks started directly and five walked into melee range first.
   - **Change:** Add `find_training_npc_XS` / `_XXS`, and consider `attack_training_npc_XS` or `attack_nearest_training_npc_XXS` that finds a suitable reachable NPC and starts the attack in one server-authoritative action. When `attack_npc_XXS` fails for reachability, return a compact recovery hint or alternate reachable target so the agent does not need a separate full target-acquisition call.
   - **Expected efficiency:** Medium-high. It removes a repeated two-call pair around every kill.

2. **Dedupe Combat Cleanup Calls And Trust Cleanup State**

   - **Evidence:** The original compact cleanup primitive is now active, so the old "add cleanup" backlog item was retired. Fresh 2026-05-27T16:10Z-16:50Z evidence shows the remaining cleanup waste: 134 `combat_cleanup_XS` calls for about 419K characters, usually two back-to-back cleanup calls per kill split between a bones-only call (`itemIds:[526]`) and a broad loot-whitelist call, followed by repeated full `observe_state` calls before target selection. In 2026-05-27T16:50Z-19:24Z, cleanup grew to 336 `combat_cleanup_XS` calls for about 1.15M characters: exactly 168 bones-only calls and 168 broad whitelist calls, with 167 immediate bones-then-broad pairs and 160 paired cleanups followed by two immediate full observes. In 2026-05-27T19:24Z-21:24Z, cleanup repeated the same shape with 270 `combat_cleanup_XS` calls for about 921K characters: 135 bones-only and 135 broad whitelist calls, 135 immediate bones-then-broad pairs, and 126 paired cleanups followed by two immediate full observes.
   - **Fresh audit:** In 2026-05-27T21:24Z-23:24Z, cleanup repeated the same paired-call shape with 278 `combat_cleanup_XS` calls for about 954K characters: 139 bones-only calls, 139 broad whitelist calls, 139 immediate bones-then-broad pairs, and 131 paired cleanups followed by two immediate full observes. In 2026-05-27T23:24Z-2026-05-28T01:24Z, the same 278-call cleanup shape cost about 946K characters with the same 139 paired cleanups and 131 paired cleanups followed by two immediate full observes.
   - **Change:** Fold bones and loot pickup into one cleanup policy per kill and make the returned compact state authoritative enough for the runner to skip immediate full observes when no inventory or equipment policy is pending. If the runner needs a second pass, return `nextCleanupNeeded`, `leftItems`, or `needsFullObserve` instead of issuing a duplicate broad cleanup by default. Also return `tool:"combat_cleanup_XS"` and an action-oriented message in cleanup results; the fresh tail still showed cleanup results identifying themselves as `combat_state_XS`, which makes it harder for runners to trust that cleanup actually ran.
   - **Expected efficiency:** Medium-high. The base cleanup batching is done, but deduping paired calls and trusting the response removes repeated compact payload plus the full observes that follow it.

3. **Expand And Actively Advertise XXS Confirmation Tools**

   - **Evidence:** High-volume simple actions included `set_combat_style`, `walk_path_steps`, `set_run`, and `click_interface_button`. After XXS became available, the active loop still mostly used full or XS tools; `combat_state_XXS` appeared, but most matching confirmation-only aliases did not. After the compact-tool prompt was given, the live 2026-05-26T17:26Z-17:55Z window showed partial adoption with 15 `attack_npc_XXS` calls and one `set_combat_style_XXS`, but full outputs still made up about 54% of returned JSON characters because movement, observe, bank, and UI calls stayed on legacy surfaces. By 2026-05-26T18:50Z-19:50Z, compact adoption improved to about 63% of returned JSON characters and 70 `attack_npc_XXS` calls, but full movement and bank/UI surfaces still dominated the remaining full-output spend. By 2026-05-26T19:50Z-20:50Z, compact adoption improved again to about 79% of returned JSON characters with 74 `attack_npc_XXS` calls; the remaining full spend is now concentrated in movement, observe, and bank/UI. In the 2026-05-26T20:50Z-21:52Z window compact share slipped to about 62% because full movement, full observe, and full bank/UI calls increased again. In 2026-05-26T21:52Z-22:51Z, compact share fell to about 51% due to repeated full restock/movement bursts despite continued `attack_npc_XXS` use. In 2026-05-26T22:51Z-23:50Z, compact share recovered to about 63%, but full movement/restock calls still contributed about 829K returned characters. In 2026-05-26T23:50Z-2026-05-27T00:50Z, compact share improved to about 74%; a new repeated full confirmation-only pattern appeared with seven `cancel_current_action` calls costing about 55K characters.
   - **Recent evidence:** In 2026-05-27T00:50Z-02:50Z, compact share fell back to about 62% because full movement/restock grew again; `cancel_current_action` repeated seven more times for about 53K characters, and `click_interface_button` repeated 16 times for about 119K characters despite being a simple withdraw-mode confirmation. In 2026-05-27T02:50Z-03:50Z, compact share stayed low at about 63%, with another eight full withdraw-mode clicks and one full `cancel_current_action`. In 2026-05-27T03:50Z-04:50Z, compact share dropped to about 57% as full movement, observe, and bank/UI grew again. In 2026-05-27T04:50Z-05:50Z, compact share fell to about 8% because the agent shifted from combat to a gear/shop upgrade workflow where most movement, observe, shop, buy/sell, equip, bank, and interface calls still used full outputs. In 2026-05-27T05:50Z-06:50Z, compact share recovered only to about 35%; full movement and observe still outweighed compact combat despite successful `attack_npc_XXS` adoption. In 2026-05-27T07:01Z-07:50Z, compact share fell to about 1.6% because object-transition/routing workflows used legacy full tools almost exclusively. In 2026-05-27T07:50Z-08:50Z, compact share was still only about 9.8%; `wait_ticks` alone produced about 323K full-output characters despite XXS wait variants existing. In 2026-05-27T08:50Z-09:50Z, compact share stayed at about 9.4%; repeated confirmation-only full tools included 75 `wait_ticks`, 34 `click_interface_button`, 10 `set_run`, five `withdraw_bank_items`, and four `deposit_inventory_items`. In 2026-05-27T09:50Z-10:50Z, compact share rose only to about 17%; full confirmation-only calls still included 41 `wait_ticks`, 27 `click_interface_button`, nine `set_run`, four `withdraw_bank_items`, and four `deposit_inventory_items`, while only one `wait_until_idle_XXS` and one `cancel_current_action_XXS` appeared. In 2026-05-27T10:50Z-11:50Z, compact share slipped to about 15.7%; full confirmation-only calls still included 36 `click_interface_button`, 28 `deposit_inventory_items`, 21 `wait_ticks`, five `set_run`, four `withdraw_bank_items`, four `cancel_current_action`, and one `deposit_excess_coins`. In 2026-05-27T11:50Z-12:50Z, compact share rose to about 29.2% only because smart XS polling exploded; full confirmation-only calls still included 41 `wait_ticks`, 28 `deposit_inventory_items`, 27 `click_interface_button`, 25 `cancel_current_action`, eight `set_run`, and four `withdraw_bank_items`. In 2026-05-27T12:50Z-13:50Z, compact share stayed around 28.1%; full confirmation-only calls included 47 `wait_ticks`, 28 `deposit_inventory_items`, 26 `click_interface_button`, seven `set_run`, four `withdraw_bank_items`, two `cancel_current_action`, and one `deposit_excess_coins`.
   - **Latest audit:** In 2026-05-27T13:50Z-14:50Z, compact share fell to about 10.5% of returned JSON characters. Full confirmation-heavy calls still included 57 `wait_ticks`, 60 `wait_until_idle`, 34 `click_interface_button`, 33 `deposit_inventory_items`, 10 `set_run`, four `withdraw_bank_items`, and three `cancel_current_action` calls. In 2026-05-27T14:50Z-15:50Z, compact share was still only about 13.3%; full confirmation-heavy calls included 251 `wait_ticks`, 39 `wait_until_idle`, 31 `click_interface_button`, 31 `deposit_inventory_items`, seven `set_run`, four `withdraw_bank_items`, and three `cancel_current_action` calls. In 2026-05-27T16:10Z-16:50Z, compact share was about 12.0%; full confirmation-heavy calls still included 56 `cancel_current_action` calls for about 500K characters, 44 `wait_ticks` calls for about 714K, 12 `click_interface_button` calls, 14 `deposit_inventory_items` calls, and one `deposit_excess_coins` call. In 2026-05-27T16:50Z-19:24Z, compact share rose to about 29.0%, but full confirmation-heavy calls still included nine `wait_ticks`, 16 `click_interface_button`, 20 `deposit_inventory_items`, four `withdraw_bank_items`, two `deposit_excess_coins`, and one `cancel_current_action`. In 2026-05-27T19:24Z-21:24Z, compact share stayed around 29.8%; full confirmation-heavy calls still included four `wait_ticks`, 12 `click_interface_button`, 15 `deposit_inventory_items`, three `withdraw_bank_items`, and one `deposit_excess_coins`.
   - **Fresh audit:** In 2026-05-27T21:24Z-23:24Z, compact calls were about 62.5% of calls but only 30.1% of returned JSON characters; full confirmation-heavy calls still included eight `wait_ticks`, 12 `click_interface_button`, 15 `deposit_inventory_items`, three `withdraw_bank_items`, and two `deposit_excess_coins`. In 2026-05-27T23:24Z-2026-05-28T01:24Z, compact calls were about 61.8% of calls but only about 29.2% of returned JSON characters; full confirmation-heavy calls still included nine `wait_ticks`, 16 `click_interface_button`, 20 `deposit_inventory_items`, three `withdraw_bank_items`, and two `deposit_excess_coins`.
   - **Change:** Add or advertise `set_combat_style_XXS`, `set_run_XXS`, `walk_path_steps_XXS`, `click_interface_button_XXS`, and `cancel_current_action_XXS`. Add a bridge-side hint or agent instruction that successful compact calls mean matching XXS aliases should be preferred for repeated confirmation-only actions.
   - **Expected efficiency:** Medium. Individual calls are smaller than full observe/movement, but these actions repeat heavily in combat and navigation loops.

4. **Make Bank Setup And Bank Policy Idempotent**

   - **Evidence:** The agent repeatedly clicked withdraw-mode button `151` and sometimes failed with `The player must be banking to change bank withdraw mode.` Excess-coin handling also appeared as a separate full-output bank-policy call around restocks. From 2026-05-26T17:55Z-18:50Z, the live session added 12 more full `click_interface_button` calls and four full `deposit_inventory_items` calls during restock handling even though compact bank variants exist.
   - **Recent evidence:** In 2026-05-27T00:50Z-02:50Z, all 16 full `click_interface_button` calls used `buttonId:21011` and returned `Set bank withdraw mode to items`, suggesting withdraw-mode setup should be an idempotent no-op inside the bank/restock primitive. In 2026-05-27T02:50Z-03:50Z, the same button repeated eight more times. In 2026-05-27T03:50Z-04:50Z, it repeated 10 more times. In 2026-05-27T04:50Z-05:50Z, bank setup alternated six full item-mode clicks and four full note-mode clicks, then hit four `withdraw_bank_items` failures for missing sale items and two out-of-bank withdraw-mode failures. In 2026-05-27T05:50Z-06:50Z, 16 more full `buttonId:21011` clicks appeared, six of them failing because the bank interface had already closed. In 2026-05-27T07:01Z-07:50Z, another 19 full interface-button calls appeared, including 14 `buttonId:21011` bank-mode attempts and six failures. In 2026-05-27T07:50Z-08:50Z, 27 more full interface-button calls appeared, including 22 `buttonId:21011` attempts and six more bank-mode failures. In 2026-05-27T08:50Z-09:50Z, 34 full interface-button calls cost about 284K characters; 30 were `buttonId:21011`, four were `buttonId:151`, and two failed after the player was no longer banking. In 2026-05-27T09:50Z-10:50Z, 27 more full interface-button calls cost about 241K characters; 22 were repeated `buttonId:21011` item-mode clicks and five were `buttonId:151`. In 2026-05-27T10:50Z-11:50Z, 36 full interface-button calls cost about 300K characters, including 30 `buttonId:21011` attempts and two out-of-bank failures; 28 full `deposit_inventory_items` calls added about 242K characters, and 22 of those deposited zero items. In 2026-05-27T11:50Z-12:50Z, 27 full interface-button calls cost about 244K characters, including 24 repeated `buttonId:21011` item-mode clicks; another 28 full `deposit_inventory_items` calls cost about 252K characters, and 24 of those deposited zero items. In 2026-05-27T12:50Z-13:50Z, 26 full interface-button calls cost about 243K characters, including 24 repeated `buttonId:21011` item-mode clicks; 28 full `deposit_inventory_items` calls cost about 261K characters, and 24 of those deposited zero items.
   - **Latest audit:** In 2026-05-27T13:50Z-14:50Z, 34 full interface-button calls cost about 322K characters; 30 were repeated `buttonId:21011` item-mode clicks. Another 33 full `deposit_inventory_items` calls cost about 311K characters, and 30 of those deposited zero items. In 2026-05-27T14:50Z-15:50Z, the same bank no-op pattern repeated: 31 full interface-button calls cost about 298K characters, including 28 `buttonId:21011` item-mode clicks, and 31 full deposits cost about 297K characters, with 28 depositing zero items. In 2026-05-27T16:10Z-16:50Z, bank no-ops were still visible: all 12 full `click_interface_button` calls used `buttonId:21011`, 14 full deposits ran, 12 deposited zero items, and one full `deposit_excess_coins` call ran. In 2026-05-27T16:50Z-19:24Z, all 16 full `click_interface_button` calls again set `buttonId:21011`; 20 full deposits ran, and 16 deposited zero items. In 2026-05-27T19:24Z-21:24Z, all 12 full bank-mode clicks again used `buttonId:21011`; 15 full deposits ran, and 12 deposited zero items.
   - **Fresh audit:** In 2026-05-27T21:24Z-23:24Z, all 12 full bank-mode clicks again used `buttonId:21011`; 15 full deposits ran, and 12 deposited zero items. These no-op bank calls cost about 234K characters before counting the surrounding observes and movement. In 2026-05-27T23:24Z-2026-05-28T01:24Z, all 16 full bank-mode clicks again used `buttonId:21011`; 20 full deposits ran, and 16 deposited zero items, costing about 313K characters before surrounding route and observe calls.
   - **Change:** Fold desired withdraw mode, quantity, `depositExcessCoins`, and `coinReserve` into `withdraw_bank_items`, `deposit_inventory_items_XS`, `food_bank_XS`, or the proposed restock primitive. Repeated requests for the current bank mode should be cheap no-ops with `isBanking`, `openInterface`, and `withdrawMode` in compact responses.
   - **Expected efficiency:** Medium. This removes repeated UI and bank-policy calls and prevents failure loops from falling back to full observations.

5. **Add Compact Shop And Equipment Upgrade Workflows**

   - **Evidence:** The 2026-05-27T04:50Z-05:50Z gear upgrade trip used full-output shop and equipment primitives: seven `open_nearest_shop` calls cost about 57K characters, three `buy_shop_item` calls about 23K, four `sell_inventory_items` calls about 32K, and eight `equip_item` calls about 55K. In 2026-05-27T05:50Z-06:50Z, shield shopping added another full `open_nearest_shop`, three full `buy_shop_item` calls, and one full `equip_item`, including two full failed buy attempts. These individual tools are smaller than movement/observe, but the workflow also caused repeated full route, bank, and interface calls.
   - **Change:** Add `open_nearest_shop_XS`, `buy_shop_item_XS` / `_XXS`, `sell_inventory_items_XS` / `_XXS`, and `equip_item_XXS`, or a higher-level `gear_upgrade_trip_XS` that routes to shops, opens/buys/sells/equips, and returns compact deltas for coins, inventory, equipment, and failures.
   - **Expected efficiency:** Medium. Gear upgrades are less frequent than combat waits, but when they happen they currently collapse compact adoption and create large full-output bursts.

6. **Add Side-Aware Object Transition Hints**

   - **Evidence:** Object navigation encountered repeated side/reachability issues such as visible-but-not-reachable objects, wrong height level, cardinal-step restrictions, and movement oscillation around dungeon gates. In 2026-05-27T05:50Z-06:50Z, `travel_to_landmark_until_arrived_XS` failed with `Landmark travel oscillated around 2946,3369,0` while targeting the Falador shield shop from the bank area, so compact travel still needs better stuck/approach hints. In 2026-05-27T07:01Z-07:50Z, the object route hit five full `walk_to_tile_until_arrived` oscillations, one `find_nearest_object` miss, and a full-object stack of `preview_local_path -> walk_to_tile_until_arrived -> interact_object -> observe_state -> walk_path_steps -> wait_until_idle` around doors, web, ladder, and manhole transitions. In 2026-05-27T07:50Z-08:50Z, the same object-route stack continued without major failures but still burned large full payloads: 39 `interact_object`, 39 `wait_until_idle`, 28 `walk_path_steps`, and 142 full observes.
   - **Change:** Add `object_transition_hint_XS` or extend `find_nearest_object_XS` failures with side/approach candidates, object height, cardinal step suggestions, and known transition-family references. Consider a route-level `object_transition_chain_XS` that batches approach, object click, proof walk, and idle wait for known transition families instead of returning full player state after every phase.
   - **Expected efficiency:** Medium. It mostly helps when route/object loops are stuck, but those stuck loops can otherwise burn many path previews, object clicks, and full observations.

7. **Roll And Index Long Agent Sessions**

   - **Evidence:** The active bridge session grew beyond 10,000 events, about 88MB raw JSONL, and more than 2MB Markdown. Later audits saw the same session exceed 100MB; by 2026-05-27T13:51Z it was about 117MB raw JSONL and 3.36MB Markdown across 13,416 events, by 2026-05-27T14:52Z it reached about 141MB raw JSONL and 3.89MB Markdown across 15,541 events, by 2026-05-27T15:51Z it reached about 162MB raw JSONL and 4.38MB Markdown across 17,541 events, by 2026-05-27T16:50Z it reached about 171MB raw JSONL and 4.59MB Markdown across 18,447 events, by 2026-05-27T19:26Z it reached about 182MB raw JSONL and 4.98MB Markdown across 20,095 events, and by 2026-05-27T21:25Z it reached about 191MB raw JSONL and 5.28MB Markdown across 21,374 events. Heartbeat audits increasingly needed targeted summaries instead of accidental full-log reads.
   - **Fresh audit:** By 2026-05-28T01:25Z, the same active session had grown to about 210.2MB raw JSONL, 5.94MB Markdown, and 24,132 events.
   - **Change:** Add size/time-based session log chunking or a lightweight hourly index. `agent_session_XS.py --latest` should read the newest chunk plus compact index by default, and full Markdown should roll up repeated phases.
   - **Expected efficiency:** Medium. This primarily saves monitoring/audit context rather than active gameplay-agent context, but the raw savings are large.

8. **Add Server-Wide Compact Kill And Loot Stats**

   - **Evidence:** Combat progress questions such as total kills, total XP, loot, food cost, bones buried, deaths, and trip yield currently require parsing noisy runner-specific logs or loading bulky state snapshots. Runner-local stats also miss normal gameplay kills that happen outside a specific combat script.
   - **Change:** Add generic server-wide combat/loot stat logging for all normal gameplay kills, not just one runner. Emit one compact structured JSONL record per NPC kill keyed by profile/session with timestamp, player, NPC id/name, kill tile/area, combat style, XP before/after/delta by skill, HP/food before/after when observable, food consumed during the kill if available, death/safety flags, drops generated with item id/name/amount, items picked up or left, bones buried, and later bank/deposit outcome when tied to that loot. Keep raw per-event logs detailed if needed, but add a compact query/rollup tool that returns aggregates only: kills by NPC, total XP by skill, XP/hr, loot totals, estimated GP value, food per kill/trip, bones buried, deaths, safety banks, and per-trip summaries. Support date ranges, coordinate bounds/box filters, and NPC type filters so agents can answer "total stats from all our kills" exactly without returning noisy state.
   - **Expected efficiency:** Medium. This does not shrink ordinary action payloads, but it should remove large ad hoc log reads whenever agents or users ask for combat totals, loot value, or trip summaries.

## Low Impact

1. **Add Payload-Spend Reporting To Session Summaries**

   - **Evidence:** Manual audits repeatedly computed per-tool result size to rank context waste. `agent_session_XS.py` currently reports top tool counts but not count/total/average/max payload size by window.
   - **Change:** Add `--since` / `--minutes` and per-tool payload-size rollups to `agent_session_XS.py`.
   - **Expected efficiency:** Low direct savings, but it improves prioritization and avoids expensive manual log reads.

2. **Fix Usage-Log Surface Classification**

   - **Evidence:** `agent-navigation/.local/usage/*.jsonl` classified direct compact calls such as `combat_state_XS`, `wait_until_combat_event_XS`, and `bury_bones_XS` as `surface:"full"` when invoked through `rs-tool`; delegated XXS calls were double-counted. A fresh 2026-05-27T16:11Z usage check still showed direct `wait_until_combat_event_smart_XS`, `combat_cleanup_XS`, and `attack_npc_XXS` invocations logged as `surface:"full"`, with delegated `rs-tool_XXS -> rs-tool` details leaking into the default usage surface.
   - **Change:** Derive logged surface from the requested dynamic tool suffix (`_XXS`, `_XS`, or none). Suppress or mark delegated internal calls as implementation detail in default reports.
   - **Expected efficiency:** Low direct savings, but it makes tool-usage audits reliable.

3. **Expose A Compact Capability Manifest**

   - **Evidence:** An active agent tried `combat_state_XXS` before the live bridge supported it and received `Unknown RuneScape agent tool`. Later, compact adoption remained hard to interpret because the audit could not tell whether the agent had a stale schema or simply chose not to use aliases.
   - **Change:** Add `bridge_capabilities_XS` or include `compactAliasesVersion`, supported suffixes, and advertised compact tools in a compact observation. Unknown `*_XXS` tools should return a structured fallback hint such as `fallbackTool:"combat_state_XS"`.
   - **Expected efficiency:** Low-medium. It prevents wasted probes and misdiagnosis, but it is not a high-volume payload reducer.

4. **Link Bridge Sessions To Codex Rollouts**

   - **Evidence:** Bridge logs clearly showed active gameplay, but matching live Codex rollout files were not visible under the expected `~/.codex/sessions/2026/05/26` path. Audits had to infer agent behavior from tool traces alone. At 2026-05-26T16:49Z, the newest local rollout files matching the date were guardian/approval or unrelated project sessions, while the active Mr. Flame bridge session was visible only through `agent_session_XS.py --latest`.
   - **Recent evidence:** During the 2026-05-27T00:50Z-02:50Z audit, the newest rollout by mtime was unrelated approval-review traffic while the Mr. Flame bridge session continued to update through `agent_session_XS.py`, so active gameplay still cannot be matched cheaply to a rollout transcript. During 2026-05-27T02:50Z-03:50Z, the newest rollout by mtime was this monitoring thread rather than the active gameplay agent. During 2026-05-27T03:50Z-04:50Z, the newest rollout candidates were again this monitoring thread or unrelated Robinhood/guardian sessions. During 2026-05-27T04:50Z-05:50Z, the newest rollout by mtime was again the monitoring thread, with pasted bridge-log output rather than a clean active-gameplay transcript. During 2026-05-27T07:01Z-07:50Z, the active bridge log rolled to `2006Scape Server/data/logs/agent-sessions/2026-05-27/`, but the newest Codex rollout mtime candidates were still older-date files, so the active gameplay transcript remains hard to identify cheaply. During 2026-05-27T07:50Z-08:50Z, the same mismatch persisted: the bridge log clearly updated, while newest rollout mtimes pointed to older-date Codex session files. During 2026-05-27T08:50Z-09:50Z, the active bridge session updated to `2026-05-27T09:52Z`, but the newest rollout mtime candidates still pointed primarily at older-date session files, so rollout matching remains unreliable. During 2026-05-27T09:50Z-10:50Z, the active bridge session updated to `2026-05-27T10:50Z`, but the newest rollout mtime candidates still pointed at older-date files rather than a clean current gameplay transcript. During 2026-05-27T10:50Z-11:50Z, the active bridge session updated to `2026-05-27T11:51Z`, while the newest rollout mtime candidates still pointed at older-date files. During 2026-05-27T11:50Z-12:50Z, the active bridge session updated to `2026-05-27T12:50Z`, while newest rollout mtime candidates still pointed at older-date files. During 2026-05-27T12:50Z-13:50Z, the active bridge session updated to `2026-05-27T13:51Z`, while newest rollout mtime candidates still pointed at older-date files. During 2026-05-27T13:50Z-14:50Z, the active bridge session updated through `2026-05-27T14:52Z`, while newest rollout mtime candidates still pointed at older-date files. During 2026-05-27T14:50Z-15:50Z, the active bridge session updated through `2026-05-27T15:51Z`, while newest rollout mtime candidates still pointed at older-date files. During 2026-05-27T16:10Z-16:50Z, the bridge session updated through `2026-05-27T16:50Z`, while the newest rollout mtime still pointed at an older-named monitoring-thread file rather than a clean active-gameplay transcript. At 2026-05-27T19:19Z, the newest rollout was a separate `mrflame-health-check` heartbeat that only reported health; the active bridge gameplay stream continued through `2026-05-27T19:26Z`, so the long gameplay loop still lacks a cheap rollout link. At 2026-05-27T21:21Z, the newest rollout was still the health-check heartbeat, while the bridge gameplay session continued through `2026-05-27T21:25Z`.
   - **Fresh audit:** At 2026-05-28T01:15Z, the newest rollout was still the separate `mrflame-health-check` heartbeat, while the bridge gameplay session continued through `2026-05-28T01:25Z`, so active gameplay is still not cheaply linked to a live rollout transcript.
   - **Change:** Record Codex session id, thread id, or rollout path in `AgentSessionLog` at session start when available, and expose it in `agent_session_XS.py --latest`.
   - **Expected efficiency:** Low direct savings, but it reduces audit context and makes root-cause analysis faster.

5. **Normalize Compact Reader Shapes**

   - **Evidence:** `agent_session_XS.py` sometimes reported `hp:null`, `food:0`, or nested compact player objects for compact tool results even though the raw result contained useful state. The 2026-05-26T16:47Z session reproduced this with `observe_state_if_changed_XXS`, `combat_state_XXS`, `combat_cleanup_XS`, and `wait_until_combat_event_smart_XS`, where the compact `player` object was treated as a `tile` value in the summary. In the 2026-05-27T02:50Z-03:50Z audit, the latest compact reader still showed `hp:null` with a nested `tile` object that contained the real HP/tile fields. In 2026-05-27T03:50Z-04:50Z, recent `attack_npc_XXS` failures still showed `hp:null` / `food:0` at the top level while the nested compact player object contained the real HP and food. At 2026-05-27T14:52Z, a recent `observe_state_XXS` summary still showed `hp:null` / `food:0` with the real HP, food, run, and tile nested inside the compact player object. At 2026-05-27T15:51Z, recent `wait_until_combat_event_smart_XS` summaries still showed top-level `hp:null` / `food:0` while the nested compact player object contained HP, run, tile, combat level, and a full skills map. At 2026-05-27T16:50Z, recent smart-wait and cleanup summaries still showed top-level `hp:null` / `food:0`, while the nested compact player object carried the real HP, free slots, run energy, tile, and skills. At 2026-05-27T19:26Z, `agent_session_XS.py --latest` still reported `hp:null` / `food:0` at the top level while nested compact state contained 44/52 HP, 16 free slots, one food, run energy, and tile. At 2026-05-27T21:25Z, the latest full-observe-derived `current` summary was normalized, but recent compact smart-wait and cleanup entries still had top-level `hp:null` / `food:0` with the usable HP/food nested under the compact player object.
   - **Fresh audit:** At 2026-05-28T01:25Z, `agent_session_XS.py --latest` again reported top-level `hp:null` and `food:0`, while the nested compact state contained the real tile, 52/54 HP, 22 free slots, one food, run energy, and other usable survival state.
   - **Change:** Teach compact readers to normalize full player objects and compact player objects, including `inventory.food` / `inventory.heal`.
   - **Expected efficiency:** Low direct savings, but it prevents auditors from opening full raw JSONL just to recover state.

6. **Standardize Recent-XP Field Names**

   - **Evidence:** Compact outputs used both `xp` and documented `xpRecent` / `skillChanges` language. Prayer also needs clear `points` versus XP-derived `base` semantics.
   - **Change:** Standardize on `xpRecent` across XS and XXS while keeping `xp` temporarily as a compatibility alias. Preserve `ageMs`, `base`, `current`, and `points` for Prayer.
   - **Expected efficiency:** Low. The payload savings are small, but consistent fields reduce follow-up probes when agents need recent XP or Prayer updates.

7. **Add Structured Shop Failure Reasons**

   - **Evidence:** `sell_inventory_item_XS` failed with `Unable to sell Mithril scimitar.` while compact state showed the item in inventory and the player had the shopping flag; the result did not identify whether the shop refused the item, slot matching failed, or the interface was stale. In 2026-05-27T05:50Z-06:50Z, two full `buy_shop_item` failures (`Unable to buy Steel sq shield`, `Unable to buy Iron kiteshield`) likewise returned the same broad message without stock, coin, requirement, slot, or shop-buyability detail.
   - **Change:** Add `reason`, `shopOpen`, `shopName`, `matchedInventoryItem`, `slot`, `itemCountBefore`, `itemCountAfter`, and `shopBuysItem` to shop failures.
   - **Expected efficiency:** Low. Shop failures are less frequent than combat/route loops, but better failure shape prevents full-observe debugging when they happen.
